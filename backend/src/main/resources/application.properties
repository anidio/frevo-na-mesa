# Configurações do Banco de Dados PostgreSQL (Supabase)
spring.datasource.url=jdbc:postgresql://db.hgznsvcxxeuwiumjdhnm.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=f4ZerhFqM,zE9@&P7

# Configurações do Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Configurações de SSL
tomcat.https.protocols=TLSv1.2

# Mostrar SQL no console (desative em produção)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configurações adicionais do Hibernate
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Configurações de CORS
# URLs permitidas para acessar a API (separadas por vírgula)
# Pode ser sobrescrito por variável de ambiente: APP_CORS_ALLOWED_ORIGINS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://frevo-na-mesa-front.vercel.app,http://localhost:5173}

# URL base da aplicação (usada para gerar links)
# Pode ser sobrescrito por variável de ambiente: APP_BASE_URL
app.base-url=${APP_BASE_URL:https://frevo-na-mesa-backend.onrender.com}